# HOW TO USE:
# 1. Copy this file and rename it to '.env'
# 2. Replace all placeholder values (anything in <angle brackets>) with your actual values
# 3. Never commit the actual .env file to version control
# 4. Keep this .env.example file updated when you add new environment variables

# Server Configuration
PORT = 8000

# MongoDB Connection String
# Replace the following values:
# <username> - Your MongoDB username
# <password> - Your MongoDB password
# <cluster-url> - Your MongoDB cluster URL
# <database-name> - Your database name (typically 'Homestead')
# <app-name> - Your application name
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority&appName=<app-name>"
